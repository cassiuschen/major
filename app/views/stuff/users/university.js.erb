var step = new Step({
	selector: '#registStep',
	container: '#registContent'
});
step.nextStep("<%= j render partial: 'stuff/dashboard/set_college_form', locals: {colleges: @uni.colleges, university: @uni}%>");
$('#message_for_regist').fadeOut().remove();
base.init()